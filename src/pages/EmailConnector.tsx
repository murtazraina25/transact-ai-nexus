
import EmailConnector from "@/components/ingestion/email/EmailConnector";
import AppLayout from "@/components/layout/AppLayout";

const EmailConnectorPage = () => {
  return (
    <AppLayout>
      <EmailConnector />
    </AppLayout>
  );
};

export default EmailConnectorPage;
